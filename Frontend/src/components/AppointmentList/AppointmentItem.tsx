import { AppointmentItemProperties } from '../../model/Properties'

const AppointmentItem = (props: AppointmentItemProperties) => {
  return <div>Appintment Item</div>
}

export default AppointmentItem
